# ⚠️ DEPRECATED — Legacy Frontend

**This directory is deprecated.** Use `frontend-next/` instead.

The legacy HTML/CSS/JS frontend is no longer maintained and may not work
with the current backend API. It is kept only for reference.

## Migration

The active frontend is the Next.js 16 application in `../frontend-next/`.

```bash
cd ../frontend-next
npm install
npm run dev
```
